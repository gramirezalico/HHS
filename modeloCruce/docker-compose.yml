version: '3.8'

services:
  modelocruce:
    build: .
    container_name: modelocruce-service
    ports:
      - "6501:6501"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge